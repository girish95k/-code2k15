<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="src.com.hoho.android.usbserial.examples.PlaySound">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="The device is now ready to start receiving data. Ensure that you've fixed the sensor leads at your right hand region and right foot region. Connect the USB cable to the device when ready."
        android:textSize="18dp"
        android:gravity="center_horizontal"
        android:layout_margin="15dp"
        android:id="@+id/text"/>

    <ProgressBar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/text"
        android:layout_margin="15dp"
        android:id="@+id/progress"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:layout_below="@+id/progress"
        android:text="Waiting..."
        android:gravity="center_horizontal"/>

</RelativeLayout>
